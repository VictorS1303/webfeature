---
const {className, id} = Astro.props;
---

<header class={className} id={id}>
   <slot/>
</header>

<style>
    header
    {
        display: inline-flex;
        flex-direction: column;
        justify-content: center;
        text-align: center;
        font-size: var(--primary-mobile-headings-font-size);
        width: 100%;
    }
</style>