---
const {className, id} = Astro.props
---

<section class={className} id={id}>
    <slot/>
</section>

<style>
    section:not(.hero) {
        position: relative;
        height: 100vh;
        width: 100vw;
        padding-top: 80px;
    }
</style>